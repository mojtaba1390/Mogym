@model Tuple<int,int>



<tr class="attr attr-person form-group">
    <input type="hidden" name="@($"SupplimentPlanRecords[{Model.Item1}].PlanId")" value="@Model.Item2" />
    <td>
        <input class="form-control" name="@($"SupplimentPlanRecords[{Model.Item1}].Title")" placeholder="مثلا بعد از ناهار،قبل تمرین" />
        <span class="field-validation-valid" data-valmsg-for="" name="@($"SupplimentPlanRecords[{Model.Item1}].Title")" data-valmsg-replace="true"></span>
    </td>
    <td class="d-flex border-0">
        <div class="form-group" style="margin: 0px 25px 0px 54px">
            <input type="submit" value="ثبت" class="btn btn-success-card dirty">
        </div>

    </td>
</tr>